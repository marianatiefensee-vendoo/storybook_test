import { Canvas, Controls, Meta, Story } from '@storybook/addon-docs/blocks';

import * as SpacingStories from './Spacing.stories';
import './foundation-docs.css';

<Meta of={SpacingStories} />

# Spacing

Spacing in Go Flow uses a compact, token-driven scale that keeps layout rhythm predictable across components and pages.

## Playground

<Canvas of={SpacingStories.Playground} />

## Controls

<Controls of={SpacingStories.Playground} />

## Tokens

<Story of={SpacingStories.AllTokens} />
