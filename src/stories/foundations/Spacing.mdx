import { Meta } from '@storybook/addon-docs/blocks';

import { spacingSections } from '../../styles/foundation-tokens';
import './foundation-docs.css';

<Meta title="Foundations/Spacing" />

# Spacing

Spacing in Go Flow uses a compact, token-driven scale that keeps layout rhythm predictable across components and pages.

<div className="foundation-page">
  {spacingSections.map((section) => (
    <section key={section.title} className="foundation-page__section">
      <div className="foundation-page__heading">
        <h2 className="foundation-page__title">{section.title}</h2>
        <p className="foundation-page__subtitle">{section.subtitle}</p>
      </div>

      <div className="foundation-card-grid">
        {section.tokens.map((token) => (
          <article key={token.name} className="foundation-card">
            <div className="foundation-card__preview foundation-card__swatch--spacing">
              <div
                className="foundation-card__spacing-bar"
                style={{ width: token.value }}
              />
            </div>
            <div className="foundation-card__meta">
              <strong className="foundation-card__name">{token.name}</strong>
              <span className="foundation-card__description">
                {token.description}
              </span>
            </div>
          </article>
        ))}
      </div>
    </section>
  ))}
</div>
