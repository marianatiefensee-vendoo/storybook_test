import { Canvas, Controls, Meta, Story } from '@storybook/addon-docs/blocks';

import * as ElevationStories from './Elevation.stories';
import './foundation-docs.css';

<Meta of={ElevationStories} />

# Elevation

Elevation tokens provide a shared shadow scale for raised surfaces and overlays.

## Playground

<Canvas of={ElevationStories.Playground} />

## Controls

<Controls of={ElevationStories.Playground} />

## Tokens

<Story of={ElevationStories.AllTokens} />
