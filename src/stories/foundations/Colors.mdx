import { Canvas, ColorItem, ColorPalette, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as ColorsStories from './Colors.stories';
import { colorPaletteSections } from '../../styles/color-tokens';
import './foundation-docs.css';

<Meta of={ColorsStories} />

# Colors

The palette below is driven by the shared semantic design tokens used by the components in this repo. Component-local aliases should map to these values, not replace them.

## Playground

<Canvas of={ColorsStories.Playground} />

## Controls

<Controls of={ColorsStories.Playground} />

<ColorPalette>
  {colorPaletteSections.map((section) => (
    <ColorItem
      key={section.title}
      title={section.title}
      subtitle={section.subtitle}
      colors={Object.values(section.colors)}
    />
  ))}
</ColorPalette>
